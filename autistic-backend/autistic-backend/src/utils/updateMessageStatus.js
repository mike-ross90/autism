// import ChatMessageModel from '../models/chatMessage.model.js'

// // Function to update the status of a message
// export async function updateMessageStatus(messageId, newStatus) {
//   try {
//     await ChatMessageModel.updateOne({ _id: messageId }, { status: newStatus })
//     console.log(`Message status updated to ${newStatus} for message ID ${messageId}`)
//   } catch (error) {
//     console.error('Error updating message status:', error.message)
//   }
// }
